{
  "name": "codeforge",
  "version": "6.0.0",
  "private": true,
  "description": "CodeForge — a fast, private, browser-based code editor. Pure static frontend (no real backend, ever). Vite is used only as a dev server and static-copy build tool — there's nothing to bundle, since Monaco and the service worker both need stable, unhashed file paths.",
  "type": "commonjs",
  "scripts": {
    "dev": "vite",
    "start": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "serve:simple": "node server.js"
  },
  "engines": {
    "node": ">=14"
  },
  "dependencies": {},
  "devDependencies": {
    "vite": "^5.4.0"
  }
}
